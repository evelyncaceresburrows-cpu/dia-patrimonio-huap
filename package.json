{
  "name": "dia-patrimonio-huap",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio Día del Patrimonio 2026 – Hospital de Urgencia Asistencia Pública (HUAP / Posta Central)",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step required' && exit 0",
    "start": "vercel dev",
    "serve": "npx --yes serve public -p 3000"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "resend": "^4.0.1"
  },
  "devDependencies": {}
}
